export interface YearStats {
    dropped: number,
    finished: number,
    year: number
}

export interface MonthStats {
    dropped: number,
    finished: number,
    year: number,
    month: number
}
